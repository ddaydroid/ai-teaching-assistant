CREATE EXTENSION IF NOT EXISTS pgcrypto;
BEGIN;

-- TODO: Replace this stub with full schema (users, concepts, activities, mastery, attempts, events, milestones)
-- See canvas 'Minimal DB Schema' for complete DDL.

COMMIT;
